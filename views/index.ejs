<% include ./partials/header %>

<% blogs.forEach( blog => { %>
    <div>
        <h1><%= blog.title %></h1>
        <img src = "<%= blog.image %>">
        <h3><%= blog.body %></h3>
        <h4><%= blog.created %></h4>
    </div>
<% }) %>

<% include ./partials/footer %>